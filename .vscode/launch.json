{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run with tsx (watch mode)",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "args": ["tsx", "watch", "src/index.ts"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
